apiVersion: v1
kind: Service
metadata:
  name: app-ejemplo-evaluacion-service
spec:
  type: NodePort
  selector:
    app: app-ejemplo-evaluacion
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080


